#!/bin/bash
#
# Lookup MX Records for a domain
#
# USAGE:
#
#   $ mxlookup davejlong.com
#   # => davejlong.com mail is handled by 30 ASPMX4.GOOGLEMAIL.com.
#   # => davejlong.com mail is handled by 10 ASPMX.L.GOOGLE.com.
#   # => davejlong.com mail is handled by 20 ALT1.ASPMX.L.GOOGLE.com.
#   # => davejlong.com mail is handled by 30 ASPMX5.GOOGLEMAIL.com.
#   # => davejlong.com mail is handled by 30 ASPMX2.GOOGLEMAIL.com.
#   # => davejlong.com mail is handled by 20 ALT2.ASPMX.L.GOOGLE.com.
#   # => davejlong.com mail is handled by 30 ASPMX3.GOOGLEMAIL.com.

host -t mx "$1"
