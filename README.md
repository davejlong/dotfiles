# davejlong's Dotfiles
## Installation
To install clone into your user root at .dotfiles (`git clone https://github.com/davejlong/dotfiles ~/.dotfiles`) then run the Rake in the .dotfiles directions (`cd ~/.dotfiles && rake`)
